# shopping-Cart
A shopping cart in Java is a simple structure that keeps track of items a user wants to buy—often stored as a list of products with quantities—supporting operations like add, remove, view contents, and compute totals.
